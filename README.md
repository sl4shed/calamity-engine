# Calamity Engine
A modular, cross-platform engine made with C++ and SDL3.

# Build instructions 
```
git clone --recursive https://github.com/sl4shed/calamity-engine 
cd calamity-engine
# make sure you have sdl3 installed system-wide
./run.sh # build for the platform you want
```